// Note: In a production environment, credentials should be
// stored in a .env file, CI/CD pipeline secrets (e.g. GitHub Actions Secrets),
// or a secrets manager and never hardcoded in the codebase
export const config = {
    baseURL: "https://animated-gingersnap-8cf7f2.netlify.app/",
    username: "admin",
    password: "password123",
};